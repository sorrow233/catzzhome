# 更新日志

## 2.3.0 - 2026-08-15

### 品牌交互与书签识别

- Catzz 单击后才显示轻量搜索输入框，双击则直接打开壁纸选择；双击判定期间不会闪现输入框。
- 删除品牌下方旧宣传短句，改为同一行的小号日期与时间，恢复首页的克制海报层级。
- 中国大陆访问默认使用百度搜索，其余地区默认使用 Google；只读取 Cloudflare 国家代码，不申请定位权限。
- 新增站点名称、网页声明图标、Apple Touch Icon 与 Web Manifest 图标识别，并在新增书签时自动回填与预览。
- 图标按照站点声明、已知品牌库、Google favicon、根目录 favicon 和首字母逐级降级；升级时会清理旧版错误图标缓存，再缓存新的识别结果。
- Cloudflare 元数据代理增加私网地址拦截、重定向复检、超时与响应体积限制，避免书签识别成为任意网络访问入口。

## 2.2.3 - 2026-08-12

### 氛围与视觉修复

- 修复 2.2 重构后整屏深色遮罩压平壁纸色彩和景深的问题，恢复不同壁纸原有的电影模式默认值。
- 将快捷入口从自动扩张的多行网格改为底部单行轨道，避免书签覆盖壁纸人物与视觉焦点。
- 收紧品牌字号、间距和文字阴影，让标题、短句与动态诗句重新形成克制的海报层级。
- 降低雨层、玻璃底色和边框强度，增加轻量暗角但保留星空、肤色和画面细节。
- 为远程站点图标增加最终渲染失败降级，避免页面显示浏览器破图标识。

## 2.2.2 - 2026-08-12

### 安全修复

- 在 Pages Functions 入口永久拦截源码、脚本、历史备份、包配置和工程配置路径。
- 即使 Cloudflare 边缘仍保留旧版静态缓存，敏感路径也会在读取缓存前返回不可缓存的 404。
- 为私有路径保护增加参数化自动测试，并记录 Firebase 懒加载包的构建预算。

## 2.2.1 - 2026-08-12

### 发布修复

- 关闭生产环境源码映射，避免构建后的完整源码映射文件被公开访问。
- 将 Cloudflare Git 集成固定为执行 `npm run build` 并仅发布 `dist`，防止自动部署绕过正式构建边界。

## 2.2.0 - 2026-08-12

### 主要更新

- 重建 Vite 生产构建与独立 `dist` 发布边界。
- 拆分壁纸、书签、同步、国际化、弹窗、图标和动画模块。
- 重构 Firebase 生命周期、冲突处理、离线恢复和失败重试。
- 统一简体中文、繁体中文、英语、日语和韩语体验。
- 完善键盘、触屏、焦点管理和减少动态效果支持。
- 增加 ESLint、Vitest、GitHub Actions、安全响应头和依赖审计能力。
- 增加按用户 UID 隔离并校验数据结构的 Firestore Security Rules。

### 修复

- 修复静置 60 秒后诗句停止、Firebase 被终止且无法可靠恢复的问题。
- 修复快速切换壁纸时旧请求覆盖新选择的问题。
- 修复仓库备份、迁移脚本和包配置被发布到生产环境的问题。
- 修复 Service Worker 缓存列表不完整以及版本长期不更新的问题。
- 修复 SEO 语言与客户端语言选择不一致的问题。
- 修复移动端无法可靠编辑或删除书签的问题。
